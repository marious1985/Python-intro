import math


def a_triangulo (altura: float, base: float) -> float:
    return ((base*altura)/2)

def a_circulo (radio: float) -> float:
    return (math.pi*(radio**2))


print("Area de triangulo:",  a_triangulo(2, 3))
print("Area de circulo:", a_circulo(3))


